form.form(name='form', ng-submit='submit(form)', method='delete', novalidate)
	.modal-header
		button.close(type='button', data-dismiss='modal', ng-click='$dismiss()') &times;
		h3.modal-title {{ title }}

	.modal-body
		p.textarea(ng-show="related.user.role==='doctor'") Czy na pewno usunąć przypisanie konta {{ removedDoctor.firstName }} {{ removedDoctor.lastName }} do użytkownika {{ user.name }}?
		p.textarea(ng-show="related.user.role==='user'") Czy na pewno usunąć przypisanie pacjenta {{ removedPacient.firstName }} {{ removedPacient.lastName }} do użytkownika {{ user.name }}?


	.modal-footer
		button.btn.btn-primary(type='submit', focus-when='true') Wykonaj
		button.btn.btn-primary(type='button', data-dismiss='modal', ng-click='$dismiss()') Anuluj